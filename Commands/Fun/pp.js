/* eslint-disable no-unused-vars */
/* const { EmbedBuilder } = module.require("discord.js");

module.exports = {
  name: "pp",
  aliases: ["peepee", "ppsize"],
  permissions: "SendMessages",
  description: "Pp size.",
  cooldown: 0,
  ownerOnly: true,
  execute(message, args, commandName, client, Discord) {
    const target = message.mentions.members.first() || message.member;

    const random = Math.floor(Math.random() * 100);

    const ppSize = "=".repeat(random);

    const ppSizeEmbed = new EmbedBuilder()
      .setTitle("PP Size")
      .setDescription(`${target.user.username}'s pp: 8` + ppSize + "D")
      .setColor("Blue");

    message.channel.send({ embeds: [ppSizeEmbed] });
  },
}; */
